<Project Sdk="Microsoft.NET.Sdk">

  <!-- Deliberately not in Md2OneNote.sln. This is Phase 0 scaffolding: it needs OneNote running
       to do anything, it will be deleted once docs/page-schema-notes.md is filled in, and the
       shipping solution should not carry a project that cannot be exercised by CI. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <LangVersion>7.3</LangVersion>
    <RootNamespace>Md2OneNote.SchemaSpike</RootNamespace>
    <AssemblyName>SchemaSpike</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NETFramework.ReferenceAssemblies" Version="1.0.3" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="System.Xml.Linq" />
  </ItemGroup>

</Project>
